# dsh-balance-tracker bundle patch: insert the plugin row into the host composition.
# A top-level YAML array of loader patch entries; applied after every bundle layer.
- insert:
    - id: dsh-balance-tracker
      name: dsh-balance-tracker
